# URL
https://blog.gennei.coffee/

# ローカルで確認

このリポジトリは Hugo `0.151.2` (extended) に合わせています。Homebrew の場合は `brew install hugo` で最新版を入れるか、`brew upgrade hugo` で同じバージョンに揃えてから以下を実行してください。

```sh
$ sh ./scripts/start.sh
```
